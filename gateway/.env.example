# Copia a .env y rellena. NO commitear .env.
LITELLM_MASTER_KEY=sk-omnia-master-cambia-esto
POSTGRES_PASSWORD=cambia-esto
# API key de PAGO de z.ai (GLM 5.2). Sácala en https://z.ai (Coding Plan si hay volumen).
ZAI_API_KEY=
# API key de OpenAI, solo para el modelo de embeddings (text-embedding-3-small).
# z.ai/GLM no tiene embeddings vía esta cuenta — ver litellm-config.yaml.
OPENAI_API_KEY=
